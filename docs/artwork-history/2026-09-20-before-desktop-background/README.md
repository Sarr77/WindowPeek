# Earlier artwork before the desktop background

The previously published composition, kept unchanged for comparison.

- [PNG](preview.png) and [self-contained SVG](preview.svg).
- [Panel](preview-panel.png) and [window preview](preview-thumbnail.png).
- [Original builder](make_preview.py) and [checksums](sha256.json).

The builder is a historical copy with its original repository paths. To render
this archived composition directly, use `rsvg-convert preview.svg -o /tmp/windowpeek-earlier.png`.
