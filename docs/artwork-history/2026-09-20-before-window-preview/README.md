# Earlier artwork without a window preview

The previous Tokyo Night composition, kept for comparison. It uses the same
`#D898F5` accent as the [current artwork](../../../preview.png).

- [Composition](preview.png) and [SVG](preview.svg).
- [Panel](preview-panel.png) and [hover list](hover-preview.png).

These are unchanged copies. Checksums are in [sha256.json](sha256.json).
